important: The app is supposed to be lightweight, clean, fast, and reliable. If any requested change excessively violates those design principles, flag it and suggest alternatives. Optimize for pixel 8a viewport (360x800).

requested updates:
- hamburger icon and slide menu on the right side
- lighten up background colors
- exercises.yaml to have optional notes key for each exercise. add info tab along top to view those notes
- make the timer smaller to fit exactly in the log set button
- don't increment weight automatically; always populate last set
- all input boxes should highlight the entire text content for edits upon click
- up next section above this session, but collapsed by default and only showing the next set. also it should be editable - sub in a set of any exercise on the fly. that change does not modify the template.
- switch to native imperial units throughout: lbs, inches
- this session and up next: reduce row height; make both visually consistent
- refactor template handling. put in shared. each template yaml file is a routine with multiple days. the UI should show the routine heading with days underneath.

