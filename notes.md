important: The app is supposed to be lightweight, clean, fast, and reliable. If any requested change excessively violates those design principles, flag it and suggest alternatives. Optimize for pixel 8a viewport (360x800).

updates:
- for routine files: 
    - remove target_reps key
    - infer superset if exercises key has more than 1
    - remove type key

- add to readme: structure and keys for exercises.yaml, routine_files.yaml
- merge history and trend tabs in the exercise page; name it history, put the plot first and list after
- wedge RPE to right of reps
- remove per set notes
- instead of timer changing to count up clock, remove timer after it runs out. Always have a countup clock next to the log set button (every time a new set is active, it's counting up)

